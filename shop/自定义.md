- 自定义指令

  - ```
    // 注册全局之定义指令 'v-focus'
    Vue.directive('focus',{
        inserted:function (el){
       		el.focus()
        }
    })
    ```

  - ```
    directives:{
    	focus:{
    		inserted : function(el){
    			el.focus()
    		}
    	}
    }
    ```

  - 使用方法 `<input v-focus>`

